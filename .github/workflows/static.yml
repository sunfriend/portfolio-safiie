name: Build and Deploy Angular App

on:
  push:
    branches:
      - main
  workflow_dispatch:  # allows manual trigger

permissions:
  contents: write  # required to push to gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3️⃣ Install dependencies in frontend
      - name: Install dependencies
        run: npm ci
        working-directory: ./frontend   # points to the Angular project

      # 4️⃣ Build Angular project
      - name: Build Angular project
        run: npm run build -- --configuration production --base-href="/portfolio-safiie/"
        working-directory: ./frontend   # points to the Angular project

      # 5️⃣ Add SPA redirect for Angular routing
      - name: Add SPA redire
